#include <stdio.h>

int factorial(int n)
{
    if(n>0)
    return  n*factorial(n-1);
    else
    return 1;
}

int main()
{
    int x,y;
    printf("enter number");
    scanf("%d",&x);
    
   y = factorial(x);
   printf("%d",y);
    
    return 0;
}
